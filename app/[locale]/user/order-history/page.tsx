import { Metadata } from 'next';

export const metadata: Metadata = {
  title: { absolute: 'Order History | Ghadaq Plus' },
  robots: {
    index: false,
    follow: false,
  },
};

export { default } from './_client';
